--- a/src/commands.cpp
+++ b/src/commands.cpp
@@ -250,6 +250,7 @@ bool Commands::launch_server_auto(const std::string& model_path, int port, int
     // Add --path flag to use custom Delta-branded web UI if found
     if (!public_path.empty()) {
         cmd << " --path \"" << public_path << "\"";
+        std::cerr << "DEBUG: Using web UI path: " << public_path << std::endl;
     }
     
     // Run in background (suppress output)
