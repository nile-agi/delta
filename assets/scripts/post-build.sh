#!/bin/bash
# Post-build script for Delta Web UI
# This script runs after vite build completes
# Currently, post-build tasks are handled by deltaBuildPlugin in vite.config.ts
# This script exists to satisfy the build process and can be extended if needed

echo "✓ Post-build script completed"

exit 0
