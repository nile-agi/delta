export const DEFAULT_MOBILE_BREAKPOINT = 768;

/**
 * Below this width the settings window drops its floating frame and goes full-bleed.
 * Kept separate from DEFAULT_MOBILE_BREAKPOINT, which drives sidebar behaviour.
 */
export const SETTINGS_WINDOW_FULLBLEED_BREAKPOINT = 640;
